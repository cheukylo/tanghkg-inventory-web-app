import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.charleslo.inventory',
  appName: 'Inventory Management - Tang HKG',
  webDir: 'dist'
};

export default config;
